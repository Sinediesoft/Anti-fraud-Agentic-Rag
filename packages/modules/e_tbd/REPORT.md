# 模組 E 自評報告

> 說明書 S15 第 5 點：每項實測值與門檻並排，沒達標的寫清楚差多少、打算怎麼辦。
> 第五週末就要求自評，是刻意把壞消息提前 —— 第七週才說出來就沒救了。
>
> **目前狀態：S8 剛建立模組，M1–M5 還是範本的做法。** 下面的空格從 S9 開始逐步填。

## 1. 這個模組負責什麼

| 項目 | 內容 |
|---|---|
| 代號 | E |
| 平台 | 求職平台 |
| 詐騙手法 | 人頭帳戶 |
| 方案 | 付費解鎖 |
| 案件數 | 2,642（說明書 §0.2 的預估，S9 實際切出來後更新。門檻 2,000 筆）|
| 負責人 | c85016921920-design |

這一組跟另外四組有一個根本差異：**使用者交出帳戶之後，他同時是被害人，
也可能成為被調查的對象。** 說明書 S12 指定給 E 的法條是刑法第 30 條幫助詐欺與
洗錢防制法第 15 條之 2，不是其他組用的詐欺犯罪危害防制條例 —— 這個差異決定了
行動劇本的走向：除了止損與報案，還要處理帳戶止付、證據保全與法律協助。

## 2. 資料（S9 / S10）

- 從共用語料切出幾筆：
- 標籤怎麼合併的：
- 60 筆標準答案的分層抽樣方式：
- 與複核者的 kappa：　（門檻 ≥ 0.70）
- 不一致處的裁決規則：

## 3. 方法

| 環節 | 我的做法 | 為什麼這樣選 |
|---|---|---|
| M1 語料處理 | | |
| M2 截圖理解 | | |
| M3 語意檢索 | | |
| M4 判讀與抽取 | | |
| M5 Agent 與行動劇本 | | |

## 4. 分數

> 每一列都要寫模型版本。少了這欄，兩週後沒人知道那個數字怎麼來的。

| 指標 | 門檻 | 實測 | 過了嗎 | 模型版本 | 測試日期 |
|---|---|---|---|---|---|
| 去識別化抓出率 | ≥ 0.95 | | | | |
| 標註一致性 kappa | ≥ 0.70 | | | | |
| OCR 錯字率 CER | ≤ 0.15 | | | | |
| 版面判對率 | ≥ 0.85 | | | | |
| Recall@5 | ≥ 0.75 | | | | |
| 查詢延遲 p95 | ≤ 1 秒 | | | | |
| macro-F1 相對進步 | ≥ 10% | | | | |
| 共用欄位 F1 | ≥ 0.70 | | | | |
| 特有欄位 F1 | ≥ 0.60 | | | | |
| can_handle 命中 | ≥ 17/20 | 0/7 | 否 | — | 2026-09-20 |

> `can_handle` 目前回 0 是**設計如此**：`labels_canon` 還沒填，`is_configured` 為 False，
> 模組不搶案子。等 S9 填完標籤才會開始有分數。考題也還只有 7 題，S12 要補到 20 題。

## 5. 錯誤分析（S13 第 7 點，20 筆）

## 6. 已知限制

> 誠實揭露限制是專業度的指標。一份寫滿限制的報告，通常比一份只有漂亮數字的報告可信。

**1. 案件數最接近門檻的一組。** 2,642 筆對上 2,000 筆的門檻，緩衝只有 642 筆，
而這還是分組表上的預估值 —— S9 用真實標籤切出來只會更少，不會更多。
說明書 S9 已經預先給了對策：「如果你的組合篩出來不到 2,000 筆，先放寬平台定義
（例如 E 把『FB 打工社團』也算進來），不要換題目」。**這是 S9 第一天就要確認的事。**

**2. 標籤最雜的一組。** 說明書 S9：「要從一大堆罕見標籤裡撈出假求職、假貸款、
幫助詐欺，還有帶著多餘空白的變體」。其他組多半對應一兩個明確標籤，E 要跨好幾個。

**3. 系統不能、也不應該回答使用者最想問的那個問題。** 走到後期階段的使用者，
真正想知道的是「我會不會有事」。那是司法程序的事，本系統只導向 165、警方與
法律扶助基金會，不做任何法律判斷。這條界線要寫進報告，也要在介面上說清楚。

## 7. 七項交付定義

以下勾選項目已由 `make selfcheck MODULE=e_tbd` 實測（2026-09-20）。

- [x] 四個進入點都實作，通過規格測試
- [x] pack.yaml 格式正確，提到的每個檔案都存在
- [ ] 20 題考題中 can_handle 至少 17 題高於門檻　← 等 S9 填 labels_canon、S12 補到 20 題
- [x] 每個階段都有行動，高風險階段不是空的（5 個階段）
- [ ] `make run MODULE=e_tbd` 在乾淨環境能跑完
- [x] 沒有自己實作去識別化／模型呼叫／分數計算
- [x] 沒有 import 別人的模組、沒有動過 shared/ 或 app/
