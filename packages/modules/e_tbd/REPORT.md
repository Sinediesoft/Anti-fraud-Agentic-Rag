# 模組 E 自評報告

> 說明書 S15 第 5 點：每項實測值與門檻並排，沒達標的寫清楚差多少、打算怎麼辦。
>
> **目前狀態：S9 完成（題目定案、語料切分、詞表校正、判定邏輯接上）。S10 未開始。**

## 1. 這個模組負責什麼

| 項目 | 內容 |
|---|---|
| 代號 | E |
| 平台 | **Threads** |
| 詐騙手法 | 網路購物詐騙 |
| 方案 | 付費解鎖 |
| 案件數 | **4,140**（讓出 5,038 筆給 D；門檻 2,000）|
| 負責人 | c85016921920-design |

### 題目跟說明書 §0.2 不同，理由是資料

說明書寫「E：求職平台 × 人頭帳戶，2,642 筆」，那是提案階段的估計。全量語料
（194,355 筆）實測後改為 Threads：

**平台用「起點」定義，不是「只出現這個平台」。** Threads 命中 12,743 筆（6.56%，
平台第四）。其中只有 32.2% 不涉及其他平台，57.2% 會導流到 LINE。但位置分析顯示：

| 關鍵字 | 位置中位數 | 落在前 20% |
|---|---:|---:|
| Threads | **0.02** | **99.4%** |
| LINE | 0.28 | 33.7% |

兩者都出現的 7,288 筆裡 **99.5% 是 Threads 先出現**（只有 34 筆反過來）。所以這
12,743 筆的起點都是 Threads，導流到 LINE 是後續環節，整條算本模組。做 LINE 的
組員處理起點在 LINE 的案例（例如群組假投資），兩邊不衝突。

**手法選網購**：Threads 上網購類 9,191 筆、佔 72.1%，壓倒性第一，與 Threads
以貼文帶貨為主的使用型態吻合。

## 模組界線：超商物流歸模組 D（2026-09-23）

模組 D 宣告的題目是「超商店到店物流 × 假冒物流平台客服（實名認證話術）」，
與本模組重疊。量化之後談定界線。

### 重疊有多大

| | 筆數 | 占比 |
|---|---:|---:|
| E 的語料命中 D 的完整核心鏈（物流＋實名＋假客服）| 3,037 | E 的 33.1% |
| D 的核心鏈中起點在 Threads 的 | 3,107 | D 的 37.0% |

### 為什麼照「平台軸」切，不照「話術」切

D 一度提議連「假客服」「實名認證」一起歸他。實測那樣切的結果：

| D 主張的四類 | 在 E 語料的占比 |
|---|---:|
| 假客服／專員 | **70.0%** |
| 實名認證／金流驗證 | 54.1% |
| 賣貨便／店到店 | 47.9% |
| 簽署／解凍／保證金 | 19.0% |
| **四類聯集** | **79.6%** |

**E 會剩 1,867 筆，不到 2,000 門檻**，而且 12 階段流程的第 4、6、7 階全部歸零 ——
`decisive` 訊號（「正規超商物流沒有實名認證這道手續」）整個消失，模組失去攔截點。

**談定的界線：照 D 自己宣告的平台軸切。** 超商物流（賣貨便／交貨便／全家／
7-11／店到店）歸 D，那是平台；「假客服」「實名認證」是**話術**不是平台 ——
假客服在整個購物詐騙族佔 70%，是共同環節不是超商物流的專屬特徵，
而且切完之後本模組剩餘語料仍有 51.5% 提到客服、34.6% 提到實名認證。

### 界線怎麼實作（兩處要一致）

| 位置 | 作用 |
|---|---|
| `m1_corpus.EXCLUDE_PATTERN` | 切語料時排除，讓出 5,038 筆 |
| `module.can_handle` | 命中時壓到 `route_hint_min` 之下 |

兩處共用同一個 pattern。**語料排除什麼，路由就要排除什麼** ——
不一致的話，模組會認領自己語料裡根本沒有的案子。實測修正前有 5/6 題
提到賣貨便的仍被認領（0.75–0.91），因為 `negative_terms` 只扣 0.5 個 hit，
抵不過三四個正向命中。

驗證：

```
D 的地盤（賣貨便／全家／7-11）   0.30  提示，完整判讀讓給 D
E 的範圍（同話術但非超商物流）    0.48–0.93  認領
其他模組（LINE 投資／FB 投資）    0.00–0.20  不碰
```

壓到提示區間而不是歸零，是因為說明書 S7 的路由允許多模組同時命中 ——
使用者仍會看到「你可能同時也遇到這個」。

### 詞表是掃出來的，不是猜的

第一版 `EXCLUDE_PATTERN` 只寫 `賣貨便|交貨便|好賣\+|店到店`，抽樣時發現案例
用的是「全家好賣」（沒有 +）、「收貨通」、「fun 心取」、「famiport 假網址」、
「7-11 的網址」等變體 —— 界線等於沒切乾淨。掃過語料後改擋「全家」詞根：
實測剩餘語料 134 筆提到「全家」，全部是便利商店，沒有一筆是「全家人」。

只擋超商，不擋一般物流：黑貓宅急便（76 筆）、嘉里大榮（64）、郵局（237）
不在 D 宣告的平台軸內，留在本模組。


## 2. 資料（S9 / S10）

- **切出幾筆**：全量 194,355 → 起點在 Threads 12,743 → 網購類 9,187 → 去重後 **9,167**
  重建：`uv run python packages/modules/e_tbd/m1_corpus.py`
- **標籤怎麼合併的**：正規化（NFKC + 去換行/定位字元 + 去頭尾空白）後，
  `labels_canon`（15 個，≥10 筆）＋ `label_aliases`（20 個變體）一起比對。
  變體有三種來源：格式差異、承辦人打錯字（「假買家騙賣騙家詐騙」）、
  **標籤欄被填成案例標題**（「一箱沒到的菸:我在Threads購物被騙15105元」，4 筆）。
- **平台規則**：`(?i)threads|脆(上|裡|裏|的貼文)|(在|滑|用|玩|逛)脆|脆友`
  「脆」只收精確樣態——明確指 Threads 的 73 筆，誤中（脆弱／乾脆／酥脆／清脆）
  1,167 筆，直接收會引入 16 倍雜訊。
- 60 筆標準答案：**（S10 待做）**
- 與複核者的 kappa：（門檻 ≥ 0.70）**（S10 待做）**

## 3. 方法

| 環節 | 我的做法 | 為什麼這樣選 |
|---|---|---|
| M1 語料處理 | 平台 × 手法兩層篩選，規則層標籤正規化 | 平台規則要能被人工複核，所以跟詞表分開 |
| M2 截圖理解 | **（S11 待做）** 20 張模擬截圖已備 | |
| M3 語意檢索 | **（S12 待做）** | |
| M4 判讀與抽取 | 階段判斷走 `threads_stages.assess()` | 12 階段定義來自 12,743 筆全量統計，比關鍵字猜測可靠 |
| M5 Agent 與行動劇本 | playbook 五階段對應 Harm 五級 | 見下 |

### 兩個關鍵設計

**一、「是不是詐騙」與「損失多少」是兩個獨立維度，不壓成單一風險分數。**
壓成一個會出現「已經把錢匯給陌生人，卻顯示低風險」的矛盾。所以 `playbook.yaml`
的階段用 Harm（尚未付款／已付款／已重複付款／已交付帳戶控制權／帳戶遭盜用）切，
不是用流程走到第幾階。

**二、沒有「安全」這個結論。** 語料 194,355 筆全部是詐騙報案，一筆正常交易都沒有，
系統沒有資格說任何情況安全。訊號不足時走 fallback 要求補充細節，最低風險等級是
medium 不是 low。

## 4. 分數

| 指標 | 門檻 | 實測 | 過了嗎 | 測試日期 |
|---|---|---|---|---|
| 去識別化抓出率 | ≥ 0.95 | | | |
| 標註一致性 kappa | ≥ 0.70 | **待 rater2** | （S10）| |
| OCR 錯字率 CER | ≤ 0.15 | | （S11）| |
| 版面判對率 | ≥ 0.85 | | （S11）| |
| Recall@5 | ≥ 0.75 | **1.00**（20/20）| 是 | 2026-09-23 |

> ### 這個 1.00 要看懂再用
>
> **過了門檻，但這個指標在這批語料上已經飽和，不能拿來比較檢索方法的好壞。**
> 語料 9,167 筆裡 71.5% 是同一個 label，講的都是賣貨便加實名認證 ——
> 隨便撈五筆都很難全部不相關。有鑑別力的是下面那兩個數字，不是這個 1.00。
>
> 判定方式（S12，2026-09-23）：`eval/route_questions.yaml` 每題只標了一筆
> `gold_case_ids`，那是「這題從哪一筆案例改寫來的」。但一句查詢必然對應到
> 幾十筆同樣切題的案例，拿單一 gold 當正確答案，會把「撈到另一筆一樣切題的
> 案例」算成失敗 —— 量到的是低估值。所以另外做了一輪相關性判定
> （`make_retrieval_judgements.py`，判定池 = Top-5 ∪ gold，共 106 筆，
> 判定者 rater1）：106 筆裡 95 筆判相關。這是 IR 的標準做法
> （pooling + relevance judgement），指標本身仍然走 `shared.eval.recall_at_k`，
> 只是餵給它的 relevant 從一筆變成一組。
>
> | | 單一 gold R@1 | R@5 | 人工判定 R@1 | R@5 | R@10 |
> |---|---|---|---|---|---|
> | 只用 BM25 | 0.450 | 0.650 | **0.900** | 1.000 | 1.000 |
> | 只用語意向量 | 0.050 | 0.300 | 0.200 | 0.600 | 0.750 |
> | **RRF 融合（現行）** | 0.450 | 0.700 | **0.900** | **1.000** | 1.000 |
>
> 每題 Top-5 有幾筆被判相關：5 筆的有 11 題、4 筆 7 題、3 筆 2 題
> （平均 Precision@5 = **0.89**）。
>
> **三個要寫進限制的事實：**
>
> 1. **判定後 BM25 單獨跑和 RRF 融合完全一樣**（R@1 0.900、R@5 1.000），
>    在這個指標上看不出語意檢索的貢獻 —— 但如上所述，Recall@5 問的是
>    「撈不撈得回那一筆」，本來就不是語意檢索的強項，用它來判向量的去留是錯的。
>    向量那一路的 0.600 還是低估的（判定池只覆蓋 RRF 的 Top-5，
>    向量自己 Top-5 裡沒被判過的一律當不相關）。
>    要公平比較，得把三路的 Top-5 都納入判定池再判一輪；
>    要回答「向量值不值得留」，還得看它擅長的那個任務（手法分類命中率）。
> 2. **判定只有一位標註者**，跟 S10 的 kappa 是同一個問題。相關與否有主觀成分，
>    尤其判準是「同一種情況」這種寬鬆的說法。
> 3. 20 題都是從 gold 案例改寫、保留專有名詞的，對詞彙比對有利。
>
> 演進：
>
> | | Top-1 | Recall@5 | 速度 |
> |---|---|---|---|
> | 字元重疊（最早） | 5/20 | 6/20 | 254 ms/題 |
> | BM25 | 9/20 | 13/20 | 55 ms/題 |
> | **BM25 + 語意向量 RRF** | **9/20** | **14/20** | — |
>
> **09-22 寫在這裡的預測沒有成真，而且原因是兩個指標被混為一談。**
> 當時預期建完向量庫就會到 82.5%。但那個 82.5% 出自 prototype 的
> `檢索方法評估.md`，量的是**手法分類命中率** —— 撈回來的案例，它的手法標籤
> 對不對（Top-1／Top-3／前 10 名眾數投票）。這裡的 Recall@5 量的是
> **撈不撈得回特定那一筆案例**。兩個是不同的任務，數字不能對接。
>
> 09-23 向量庫建好（9,167 筆 × 1024 維、bge-m3）之後實測：
>
> | | Top-1 | Recall@5 | Recall@10 |
> |---|---|---|---|
> | 只用 BM25 | 0.450 | 0.650 | 0.650 |
> | 只用語意向量 | 0.050 | **0.300** | 0.300 |
> | RRF K=60 等權（原設定）| 0.400 | **0.600** | 0.700 |
> | **RRF K=10 + BM25 權重 2** | 0.450 | **0.700** | 0.750 |
>
> 兩件事跟預期不同：
>
> 1. **語意那一路在「撈回特定那一筆」上很弱**（單一 gold R@5 只有 0.30），
>    但人工判定之後是 **0.600 ——整整一倍**。這個落差本身就是答案：
>    它撈回來的案例確實切題，只是不是改寫來源那一筆。**語意檢索擅長找同類，
>    不擅長找同一筆**，而 Recall@5 量的正是後者。所以 0.30 不能讀成「向量沒用」，
>    只能讀成「向量不擅長這個特定任務」。
>
>    案例高度同質（71.5% 同一個 label，講的都是賣貨便加實名認證）讓這件事更明顯：
>    語意上分不開，能區辨「是哪一筆」的資訊幾乎都在專有名詞，那是 BM25 的主場。
> 2. **等權融合反而比單用 BM25 差**（0.650 → 0.600），弱的那一路把強的擠掉了。
>    調成 K=10、BM25 權重 2 之後才回到 0.700。
>
> ⚠ 這組融合參數是在同一批 20 題上觀察到的，而那 20 題從 gold 案例改寫、保留了
> 專有名詞，本來就對詞彙比對有利。它只能說「融合不再比單路差」，不等於調好了。
>
> **要真正驗證語意檢索值不值得留，需要一份專門的檢索評估集**：口語、刻意避開
> 專有名詞、每題標 `gold_case_ids`。個人 repo 的 `prototype/eval_queries.py`
> 有 40 題符合前兩項，但只標了「期望手法」而沒有標到案例編號，所以還不能拿來
> 算 Recall@5（語料 71.5% 是同一個 label，用 label 命中當指標太寬鬆，沒有鑑別力）。
| macro-F1 相對進步 | ≥ 10% | | （S13）| |
| **can_handle 命中** | ≥ 17/20 | **18/20** | **是** | 2026-09-22 |
| 階段判定 vs gold（60 筆）| — | **61.7%**（kappa 0.501）| 參考值 | 2026-09-22 |

> 最後一列不是說明書的門檻，是 S10 建完 gold 之後自己加的參考值 ——
> 有了標準答案，每次改規則都量得到，不必再對著自編題目猜。
> 過程：33.3%（kappa 0.169）→ 修四處缺陷 55.0% → 擴充重複付款標記 **61.7%**。
>
> **在這裡停手是刻意的。** 剩下的錯誤已經分散（最大一類 4 筆），沒有系統性模式，
> 繼續調就是對這 60 筆過擬合。而且 gold 目前只有一位標註者、還標了三輪，
> 它自己的可信度尚未經 rater2 驗證 —— 對著一個還沒驗證的標準繼續優化沒有意義。

### can_handle 的實測（真實案例 500 筆 vs 非 Threads 案例 500 筆）

| 門檻 | 真實案例認領 | 其他手法誤報 | 20 題考題 |
|---:|---:|---:|---:|
| 0.35 | 59% | 16% | 11/20 |
| **0.45** | **58%** | **16%** | **11/20** |
| 0.55 | 48% | 12% | 7/20 |

取 0.45：0.55 把召回壓到不到一半，換到的誤報改善只有 4 個百分點，不划算。

**為什麼決定性訊號要單獨加權**：`keyword_score` 的飽和函式對所有詞一視同仁
（1 個 = 0.33、2 個 = 0.50），但實測 lift 差距很大：

| 詞 | 出現率 | lift |
|---|---:|---:|
| 未完成 | 29.1% | **8.9** |
| 賣貨便 | 37.1% | **7.9** |
| 實名認證 | 30.0% | **7.6** |
| 客服 | 66.2% | 2.8 |

「賣貨便」的區分力是「客服」的三倍，平等對待會讓只提到一個強訊號的短查詢落榜——
而真實使用者的第一句話往往就只有那一個訊號。所以 lift ≥ 6 的詞命中一個就給 0.60。

## 5. 錯誤分析（S13 第 7 點，20 筆）

## 6. 已知限制

**1. 熱度已過高峰，報告的論述角度要調整。**

| 季 | Threads 佔當季 |
|---|---:|
| 2025Q4 | **13.86%**（3,542 筆，高峰）|
| 2026Q3 | 5.16%（1,084 筆）|

絕對筆數掉 **69%**，同期全體只掉 18%——是 Threads 特有的衰退。
**不能寫成「Threads 是當前最嚴重的平台」，數據會反駁。** 可行方向：以 2025Q4
爆發期為研究對象分析新平台上線初期的詐騙模式，或探討衰退原因。

**2. ~~can_handle 只有 11/20~~ 已解決（2026-09-22）。**
先前判斷「不該靠調參數解決，要先做 S10 再從真實案例出題」是對的——
S10 完成後照做，11/20 → **18/20**，而且誤報率同時從 16% 降到 **0%**。

過程留一個教訓：改寫 gold 案例成考題時，我一度把平台詞省略以聚焦話術，
結果造出現實中不存在的題目（gold 有 94% 提到平台），考題一度掉到 10/20。
**從真實案例改寫時，不能為了讓句子「乾淨」而拿掉真實案例一定會有的成分。**

**3. 路由考題與檢索評估集不能共用。** 檢索評估集刻意避開專有名詞（否則詞彙比對
會不公平地佔便宜），但 `can_handle` 正是靠那些詞區分類別——實測把檢索評估集拿來
測路由會得到 0/20。兩份題目分開放。

**4. 語料無負樣本，無法量測誤判率。** 194,355 筆全是報案案例，沒有一筆「正常交易」。
所以系統只能做風險分級，不能做詐騙認定。

**5. 平台推導規則未經有效的人工複核。** 2026-09-21 做過一次 300 筆盲標
（命中集 200＋未命中集 100），結果是 193/193 全對、精確率 100%。
**但那次的標註語意是「這筆是不是詐騙」而不是「這筆是不是發生在 Threads」**——
165 全是報案案例，問前者每一筆都是「是」，等於沒有驗證到平台規則。要重做。

未命中集 100 筆全被標成「看不出來」，所以**漏抓率分母為 0、無法計算**（不是 0%）。
另外 `(?i)threads` 可能誤中英文 thread（討論串）的複數形。

**6. 漏抓率無法估計。** 案例文本沒寫平台名時，人工也判不出來——這是資料天花板。

**7. 標準答案與系統實際使用的時間點不同，`credentials` 會被系統性低估。**

`credentials`（已交付帳戶控制權）與 `drained`（帳戶遭盜用）在實務上高度相關 ——
9,167 筆裡有 2,653 筆命中交出控制權的訊號，其中 **35.7% 最後真的被轉走**，
明確描述「及時掛失／停卡止損」的只有 **102 筆（3.8%）**。
其餘 1,705 筆沒寫到資金流出，但**「沒寫到」不等於「沒發生」**——
165 的案例是報案當下的陳述，很多人寫到「我才發現被騙」就結束了。

問題在兩邊的時間點：

| | 看到的是 | 傾向判 |
|---|---|---|
| 標註者 | 完整案例，已知結局 | `drained` |
| 系統實際使用時 | 使用者此刻的處境，結局未定 | `credentials` |

真實使用者打字進來多半是「我剛剛把驗證碼給他了，怎麼辦」——那正是
`credentials`，也是這個工具最有價值的攔截點（還來得及打銀行停卡圈存）。
但標準答案會把同一類案例標成 `drained`，所以評估時 `credentials` 的分數
會偏低。**那是評估方法的侷限，不是模組判錯。**

兩級不合併，因為行動清單本質不同：`credentials` 是「阻止」（立刻停卡），
`drained` 是「追償」（報案、申請圈存收款帳戶）。就算多數案例最終會被轉走，
在還沒轉走的那個當下，給「立刻停卡」仍然是正確且可能有效的建議。

## 7. 七項交付定義

以 `make selfcheck MODULE=e_tbd` 實測（2026-09-22）。

- [x] 四個進入點都實作，通過規格測試
- [x] pack.yaml 格式正確，提到的每個檔案都存在
- [ ] 20 題考題中 can_handle 至少 17 題高於門檻　← 等 S10 從真實案例重新出題
- [x] 每個階段都有行動，高風險階段不是空的（5 個階段）
- [ ] `make run MODULE=e_tbd` 在乾淨環境能跑完
- [x] 沒有自己實作去識別化／模型呼叫／分數計算
- [x] 沒有 import 別人的模組、沒有動過 shared/ 或 app/
