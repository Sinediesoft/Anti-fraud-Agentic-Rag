# 模組 A 自評報告

> 說明書 S15 第 5 點：每項實測值與門檻並排，沒達標的寫清楚差多少、打算怎麼辦。
> 第五週末就要求自評，是刻意把壞消息提前 —— 第七週才說出來就沒救了。
>
> **對到 2026-09-22，分支 `feat/整合-a-c`。**
> 沒量過的欄位一律寫「未測」並附上原因，不填推估值 —— 兩週後沒人分得出
> 哪個數字是量的、哪個是猜的。

## 1. 這個模組負責什麼

| 項目 | 內容 |
|---|---|
| 代號 | A |
| 平台 | **LINE** |
| 詐騙手法 | **假投資** |
| 方案 | 免費版開放（件數最大的一組） |
| 案件數 | **17,764 筆**（門檻 2,000，約 8.9 倍） |
| 負責人 | Sinediesoft |

## 2. 資料（S9 / S10）

- **從共用語料切出幾筆**：194,355 筆 → 17,764 筆。
  先取假投資系列標籤（不分平台）30,778 筆，再取內文提得到 LINE 的，得 17,764 筆。
- **標籤怎麼合併的**：三個官方標籤併成一類 —— 假投資詐騙（27,751）、假投資（2,510）、
  假投資購物詐騙（517）。第三個偏購物但話術同源，先納入。
  165 的 `label` 欄不是受控分類，有前後空白與全形括號變體，比對前一律過
  `m1_corpus.norm_label` 正規化。
- **60 筆標準答案的分層抽樣方式**：**未做（S10）**。這是下游三件事的前提 ——
  20 題考題要從這 60 筆抽、`gold_case_ids` 要有編號可填、Recall@5 要有答案可比。
- **與複核者的 kappa**：**未測**（門檻 ≥ 0.70）。等 60 筆標完才有東西可複核。
- **不一致處的裁決規則**：未定，與上一項同步進行。

## 3. 方法

| 環節 | 我的做法 | 為什麼這樣選 |
|---|---|---|
| M1 語料處理 | 從 165 parquet 切出自己那一份，標籤正規化後比對 `labels_canon`。已從 1,000 筆抽樣切到全量 17,764 筆 | 抽樣階段量出來的門檻放到全量就不準了 —— 切全量時確實踩到一個只有全量才會出現的 bug |
| M2 截圖理解 | **尚未實作。** OCR 引擎未選定，`read_screenshot()` 走降級路徑，回 `degraded=True`、只用打字的內容判讀 | 降級而不是當掉：沒有 OCR 時整條流程仍要跑得完。但這代表截圖上傳目前等於沒作用 |
| M3 語意檢索 | bge-m3 向量檢索，**兩道門檻**：第一道看相似度，第二道看「相似度 + 手法詞加分」（`TACTIC_BONUS=0.15`，依比例給） | 單一門檻時離題問句照樣拿回案例。向量相似度分不出「像是因為都在講投資」還是「像是因為都在講出不了金」，而後者才是使用者問的那件事。加分用比例而非每命中加固定值，是為了讓問句長短不影響上限 |
| M4 判讀與抽取 | 三層退路。**第三層（規則硬抽）已實作**，信心一律標低；第一層的 SLM prompt 未寫，`_SLM_PROMPT_READY` 仍為 `False` | 規則層先上是為了讓流程端對端跑得完。信心標低是因為那不是模型讀出來的 —— 信心值說謊比判錯更糟 |
| M5 Agent 與行動劇本 | **尚未實作。** `playbook.yaml` 還是範本的 TODO | 畫面上「目前階段」「接下來該做什麼」顯示的就是那幾行 TODO 文字，等於這個模組最重要的產出還是空的 |

**已建好的索引**：17,764 × 1024 維（`index/vectors.npy`，`case_ids.json` 對得上，各自本機建、不進版控）。

## 4. 分數

> 每一列都要寫模型版本。少了這欄，兩週後沒人知道那個數字怎麼來的。

| 指標 | 門檻 | 實測 | 過了嗎 | 模型版本 | 測試日期 |
|---|---|---|---|---|---|
| 去識別化抓出率 | ≥ 0.95 | 未測 | — | — | — |
| 標註一致性 kappa | ≥ 0.70 | 未測（60 筆未標） | — | — | — |
| OCR 錯字率 CER | ≤ 0.15 | 未測（M2 未實作） | — | — | — |
| 版面判對率 | ≥ 0.85 | 未測（M2 未實作） | — | — | — |
| Recall@5 | ≥ 0.75 | 未測（無 `gold_case_ids`） | — | — | — |
| 查詢延遲 p95 | ≤ 1 秒 | 未測 | — | — | — |
| macro-F1 相對進步 | ≥ 10% | 未測 | — | — | — |
| 共用欄位 F1 | ≥ 0.70 | 未測 | — | — | — |
| 特有欄位 F1 | ≥ 0.60 | 未測 | — | — | — |
| `can_handle` 命中 | ≥ 17/20 | **19/20** | ✅ | 詞彙比對，不含模型 | 2026-09-22 |

**一句話**：十項裡唯一過了的是 `can_handle`，其餘九項的前提都卡在 S10 的
60 筆標註 —— **先做 S10 才是唯一能解開多項的動作**，單獨補任何一項都不會
讓這張表再綠一格。

`can_handle` 那一項的過程記在 6.1：同一份 20 題，機制修正前 10/20、修正後
19/20，題目一個字沒動。**沒有安全邊際** —— 10 題白話查詢裡有 5 題剛好落在
0.5000，等於 `route_min`，門檻往上動一格就會一起掉下去。

參考數字（不是門檻項，但量過）：一次判讀從 7,141ms 降到 116ms（`429ac50`），
同一個 commit 也修掉一個會說謊的信心值。

## 5. 錯誤分析（S13 第 7 點，20 筆）

未做。前提同上 —— 沒有標準答案就分不出哪 20 筆是錯的。

目前能指認的唯一系統性錯誤來自路由實測（見下節），不是從 20 筆樣本來的。

## 6. 已知限制

> 誠實揭露限制是專業度的指標。一份寫滿限制的報告，通常比一份只有漂亮數字的報告可信。

### 6.1 平台判定原本只有兩態，白話問句因此全滅（已修，但沒有安全邊際）

`can_handle()` = `keyword_score` × 平台係數。`keyword_score` 是飽和函式
`hits/(hits+2)`，而平台判定原本只有兩態：有我的平台詞乘 1.3，**其餘一律
乘 0.6**：

| 命中 | 底分 | ×0.6（無平台詞） |
|---:|---:|---:|
| 2 | 0.5000 | 0.3000 |
| 3 | 0.6000 | 0.3600 |
| 5 | 0.7143 | 0.4286 |
| 8 | 0.8000 | 0.4800 |
| 10 | 0.8333 | **0.5000** ← 要 10 個命中才剛好碰到 `route_min` |

一句口語塞不進 10 個手法詞，所以 10 題白話查詢全數落在 0.20–0.40。
本專案 README 開場的起點例句「群組裡的老師叫我先入金才能出金」只有 0.30。

**根因**：「沒提任何平台」與「提到別人的平台」被當成同一件事。
前者是資訊不足，後者是證據指向別人 —— **倒扣的理由只對後者成立**。

**改法**：三態化。有我的平台詞 ×1.3 ／ 沒提任何平台 **×1.0（中性）** ／
只提到別人的平台 ×0.6。兩邊都出現時算自己的 —— 這不是偷分，是照語料的
實際樣態：A 的 17,764 筆是「假投資 ∩ 內文提得到 LINE」切出來的，而這一類
的典型歷程就是在別處看到廣告、再被導進 LINE 談。

別人的平台詞清單放在 `module.py` 的 `OTHER_PLATFORM_TERMS`，不是 `pack.yaml`
—— `PackSpec` 設了 `extra="forbid"`，加欄位要動凍結的 `contracts/`。
清單是字面複製、不是 import，沒有違反規矩二；只收不會誤判的平台專名
（不收「社團」「超商」「脆」，理由寫在那份常數的註解裡）。

**量測（同一份 20 題，題目一個字沒動）**：

| | 總分 | 不含平台詞 | 含平台詞 |
|---|---|---|---|
| 兩態（原本） | 10/20 | 0/10 | 10/10 |
| 三態（現在） | **19/20** | 9/10 | 10/10 |

中性不是放寬：手法詞仍然要自己掙到 2 個命中才過門檻。守住的另一邊也量過
——「我在臉書看到投資廣告，對方說保證獲利，現在出金失敗」是 **0.40，A 不
認領**（提到別人的平台照舊打 0.6 的折）。

🔴 **但這個 19/20 沒有安全邊際**：白話那 10 題裡有 5 題剛好落在 **0.5000**，
等於 `route_min`（比較是 `>=`，所以過）。門檻往上動一格、或 `keyword_score`
的分母改掉，這 5 題會一起掉下去。調參數前先重跑 `eval/route_questions.yaml`。

唯一沒過的第 10 題是刻意留的：「我想把投資的錢領回來，對方一直叫我再加碼」
—— 受害者只講「領回來」「加碼」，兩個詞都不在 `route_terms` 裡。留著是為了
讓詞表的缺口看得見，當 S18 補詞時的依據。

### 6.2 出題規則本身有設計問題（要拿到全隊層級）

出題規則要求「至少 8 題完全不含專有名詞」，但 `can_handle()` 正是靠那些詞
區分類別。6.1 那個修正讓 A 過得去，但問題的根在範本。

PR #41（模組 E）獨立量到同一件事並寫下診斷：「路由考題與檢索評估集不能共用
—— 檢索評估集刻意避開專有名詞，而 `can_handle` 正是靠那些詞區分類別」，
他們混用時得到 **0/20**。

兩個人從不同方向撞到同一面牆，所以這不是各自調參數就好：建議把路由題與
檢索評估集拆成兩份檔案。那會動到範本與 `selfcheck` 讀的路徑（共用區），
要全隊同意，B／C／D／E 都得跟著改。**這條還沒提到站立會。**

### 6.3 截圖上傳目前等於沒作用

`m2_vision.read_screenshot()` 是 stub，`screenshots/` 是空的（S11 第 1 點要 20 張）。
系統不會當掉，但也不會從截圖多知道任何事。介面上若讓使用者上傳，
他會以為系統看了 —— 這是會誤導人的降級，優先度應該高於它現在的排序。

### 6.4 與 C 的邊界靠平台詞撐著，沒有量過

A（LINE × 假投資）與 C（Facebook × 投資詐騙）是同一個手法、不同平台。
2026-09-22 抽查四句，分得開：

| 問句 | A | C | 判給 |
|---|---:|---:|---|
| LINE 群組的老師叫我投資保證獲利，現在出金失敗 | 0.929 | 0.714 | A |
| 臉書社團看到飆股廣告，加了老師的 LINE 帶我下單 | 0.000 | 0.650 | C |
| 群組裡的老師叫我先入金才能出金 | 0.300 | 0.600 | C |

但這是四句抽查，不是混淆矩陣。真正的邊界品質要等 S18 用考題量。
另外 PR #41 指出 Threads 案例有 57.2% 會導流到 LINE，E 用「起點」論證兩邊不衝突
—— 這個論證要 A 這邊確認過才算數，目前還沒確認。

### 6.5 `route_min` 這三個數字是在舊機制下量的

`route_min=0.50` 是 2026-09-20 在**加法**版本（平台命中 +0.15）下量的。
2026-09-21 已改成雙邊乘法（1.3／0.6），所以原本的理由不再成立，
而這三個值還沒在乘法版本上重量過。`pack.yaml` 已標 `TODO(S18)`。

## 7. 七項交付定義

以 `make selfcheck MODULE=a_tbd` 2026-09-22 的結果為準（**7/7，可以掛載**）。

- [x] 四個進入點都實作，通過規格測試
- [x] pack.yaml 格式正確，提到的每個檔案都存在
- [x] 20 題考題中 can_handle 至少 17 題高於門檻（**19/20**，過程見 6.1）
- [x] 每個階段都有行動，高風險階段不是空的（3 個階段 —— 但內容還是範本 TODO，
      這一項機械上過了、實質上沒過，見 M5）
- [x] `make run MODULE=a_tbd` 在乾淨環境能跑完
- [x] 沒有自己實作去識別化／模型呼叫／分數計算（`tools/check_boundaries.py` 通過）
- [x] 沒有 import 別人的模組、沒有動過 shared/ 或 app/

> 第 4 項要特別說明：檢查器看的是「每個階段有沒有行動、高風險階段是否為空」，
> 範本的 TODO 文字算「有行動」，所以機械檢查會過。**這是檢查器的盲點，不是
> 這個模組已經做完了。** 寫在這裡免得驗收時被這個 ✅ 誤導。
