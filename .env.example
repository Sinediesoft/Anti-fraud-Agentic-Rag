# 複製成 .env 後填入。.env 已被 .gitignore 擋住，絕對不要 commit 進儲存庫。
# 雲端模型金鑰由 S3 決定的保管人發放，並且務必先設用量上限。

# TODO(S3)：填入全隊決議的雲端供應商與金鑰
CLOUD_LLM_API_KEY=

# 地端模型服務位置（Ollama 預設）
OLLAMA_HOST=http://127.0.0.1:11434

# 共用語料的本機路徑（語料放共用雲端硬碟，不進版控）
CORPUS_PARQUET=data/cases_165.parquet
