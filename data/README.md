# 語料放哪裡

大檔案絕對不要進 GitHub（說明書 §1.6）。這個資料夾在版控裡是空的，只留這份說明。

| 東西 | 放哪裡 | 大小 |
|---|---|---|
| 165 全量語料 `cases_165.parquet` | 共用雲端硬碟 | 幾十～上百 MB |
| 模型檔（GGUF 等） | 各自本機下載，對編號 | 幾 GB |
| 向量資料庫 | 各自本機建（`make index MODULE=…`） | 幾百 MB |

## 怎麼拿

TODO(S6)：全隊抓過一次之後，把下面三件事填清楚 ——

- **去哪裡拿**：
- **怎麼拿**：端點 `POST /CIB_DWS_API/api/CaseSummary/GetCaseSummaryList`，
  每頁 2 萬筆、共 10 頁，約兩分半跑完
- **哪天抓的**：

抓下來之後放到本機任一位置，然後在 `.env` 裡指定路徑：

```
CORPUS_PARQUET=/你的路徑/cases_165.parquet
```

## 使用界線

案例內文是真實受害者的第一人稱敘述。只在專題內部用，
展示一律用節錄或改寫，絕不投影原文。
